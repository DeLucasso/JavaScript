// 1st function does adding two inputs
function add (a, b) {
    return a + b;
}

// 2nd function does multiply two inputs
function multiply (a, b) {
    return a * b;
}

// 3rd function take two numbers and function as inputs
function calculator (a, b, operator) {
    return operator (a,b);
}
